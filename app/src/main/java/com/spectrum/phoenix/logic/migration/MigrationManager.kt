package com.spectrum.phoenix.logic.migration

import android.content.Context

class MigrationManager(private val context: Context) {
    fun migrate() {
        // Migración v12 completada exitosamente. 
        // Se ha eliminado el código de carga masiva para mantener la aplicación ligera.
    }
}
